import React from 'react';
import { Wrapper } from '../components/Wrapper';

export const Setting = () => {
	return (
		<>
			<Wrapper title="Form Setting">Nothing is configurable yet.</Wrapper>
		</>
	);
};
